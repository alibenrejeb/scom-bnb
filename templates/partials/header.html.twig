<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">ScomBNB</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_ads_index') }}">Annonces</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="accountNavbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <img src="{{ app.user.picture }}" alt="Avatar de {{app.user.fullname}}" class="avatar avatar-mini">
                        {{ app.user.fullname }}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-right" aria-labelledby="accountNavbarDropdown">
                        <li><a href="{{ path('app_ads_new') }}" class="dropdown-item">Créer une annonce</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a href="{{ path('app_account') }}" class="dropdown-item">Mon compte</a></li>
                        <li><a href="{{ path('app_account_profile') }}" class="dropdown-item">Modifier mon profil</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a href="{{ path('app_account_logout') }}" class="dropdown-item">Déconnexion</a></li>
                    </ul>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_account_register') }}">Inscription</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_account_login') }}">Connexion</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{label}}">
            {% for message in messages %}
                <p>{{ message|raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}